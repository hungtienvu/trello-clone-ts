import { AddNewItem } from "./AddNewItem"
import { render, fireEvent } from "@testing-library/react"

describe("shows from", () => {
  it("should ", () => {

    // const { container } = render(<AddNewItem />)
    
  })
})
